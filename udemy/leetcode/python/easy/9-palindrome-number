class Solution:
    def isPalindrome(self, x: int) -> bool:
        if x < 0:
            return False
        counter = len(str(x))
        for i in range(counter):
            if int(str(x)[i]) != int(str(x)[counter - i - 1]):
                return False
        return True